
## 解决方案

原生方案： Web Components
库：webpack5+的模块联邦(Module Federation)
库：qiankun
库：wujie
Server Components 融合：在2026年，观察微前端如何与 RSC (React Server Components) 结合，实现服务端层面的微前端拆分。
single-spa



核心概念：基座应用（Main App）、子应用（Sub App）、路由劫持、JS沙箱、样式隔离。
通信机制：理解主应用与子应用如何交换数据（Custom Events, Props, Global Store）。
阅读经典：阅读 single-spa（微前端鼻祖）和 qiankun 的官方文档，理解其设计哲学。



 
 
### 4.1 iframe 方案
```javascript
// 优点
- 天然隔离，CSS/JS 完全隔离
- 技术栈完全无关
- 实现简单，浏览器原生支持

// 缺点
- 用户体验差（刷新、前进后退问题）
- 通信复杂（postMessage）
- SEO 不友好
- 性能开销大
```

### 4.2 qiankun 方案
```javascript
// 基于 single-spa
// 优点
- 生产环境验证丰富
- 功能完善（沙箱、通信、预加载）
- 社区生态活跃
- 支持多种技术栈

// 缺点
- 配置相对复杂
- 子应用改造成本
- 性能有一定开销
```

### 4.3 无界微前端
```javascript
// 基于 Web Components
// 优点
- 性能优秀，接近原生
- 子应用无需改造
- 用户体验好
- 通信简单直观

// 缺点
- 相对较新，生态较小
- 某些特殊场景支持有限
```

# 基于无界微前端的项目架构升级指南
无界微前端是一种基于 Web Components 的微前端解决方案，能够帮助大型应用实现技术栈无关、独立开发部署的能力。下面我将详细介绍如何进行项目架构升级。

## 1. 理解无界微前端核心概念
### 1.1 无界微前端特点
+ **技术栈无关**：主应用和子应用可以使用不同技术框架
+ **应用隔离**：完善的 JS、CSS 沙箱机制
+ **性能优秀**：无需注册路由，直接渲染组件
+ **通信简单**：提供 props 和 event 两种通信方式
 
 ## 方案一 iframe
通过iframes进行运行时集成



## 方案二 webpack5 模块联邦
  
构建时组合 Module Federation 编译时集成，性能好 技术栈统一的大型应用  
运行时组合 qiankun, Single-SPA 运行时动态加载 多技术栈、独立部署  
Iframe 原生方案 完全隔离，简单 简单集成、需要强隔离  
Web Components 原生标准 浏览器原生支持 长期项目，关注标准



## <font style="color:rgb(48, 54, 51);">通过JavaScript的运行时集成</font>
## 方案三 Web Components
<font style="color:rgb(48, 54, 51);">通过Web Components 进行运行时集成</font>

## 方案四 qiankun
Qiankun是蚂蚁开源的微前端框架，它基于single-spa进行二次开发，旨在将Web应用由单一的单体应用转变为多个小型前端应用聚合为一的应用。以下是对Qiankun的详细介绍：

六、优势与劣势

优势：

技术兼容性好：各个子应用可以基于不同的技术架构。

接入方式简单：像使用iframe一样接入微应用。

耦合性低：各个团队可以独立开发，互不干扰。

可维护性和扩展性好：便于局部升级和增量升级。

劣势：

无法支持多实例场景。

对Vite等现代前端构建工具的支持不够友好，需要改动较多的代码。

子应用间的资源共享能力较差，可能导致项目总体积变大。

开发人员需要处理分布式系统的复杂性。



## 方案五 无界
<font style="color:rgb(51, 51, 51);">无界是腾讯开源的微前端框架，旨在解决现有微前端方案中存在的问题，如适配成本高、样式隔离困难、运行性能不佳、页面白屏、子应用通信复杂、子应用保活机制缺乏等</font>

技术实现

无界微前端的核心技术是基于Web Components容器 + iframe沙箱。通过这种方式，它可以实现以下目标：



成本低：无论是主应用还是子应用，使用成本都相对较低。

速度快：子应用首屏打开速度较快，且运行速度快。

原生隔离：通过Web Components实现了严格的样式隔离，而JavaScript则运行在iframe中，保证了代码的隔离。

功能强大：支持子应用保活、子应用嵌套、多应用激活、应用共享等功能。



无界微前端是一种基于 Web Components 的微前端解决方案

