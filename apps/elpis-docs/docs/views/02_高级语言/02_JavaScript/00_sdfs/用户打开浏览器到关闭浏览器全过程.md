



事件循环 每一轮不一定会执行渲染 但一定会判断是否需要渲染 判断是否需要渲染的条件有 dom是否改变 距离上一次是否到达了帧间隔。我这样理解对吗

你的理解完全正确。

更完整的说法：事件循环在每次清空微任务后，都会检查“是否需要渲染”，检查条件包括：是否有待处理的视觉更新（DOM/样式变化、动画回调等） 且 是否已达到预期的渲染时机（通常与刷新率对齐）。

如果条件满足，则执行渲染阶段（样式、布局、绘制、合成）；否则跳过，直接进入下一个宏任务。



同步任务(产生异步) - 清空微任务队列 - 判断是否需要渲染 - 一个宏任务 
                - 清空微任务队列 - 判断是否需要渲染 - 一个宏任务 
                - 清空微任务队列 - 判断是否需要渲染 - 一个宏任务 
                - 清空微任务队列 - 判断是否需要渲染 - 一个宏任务 - 主线程空闲 requestIdelCallback 


事件循环机制中 
主线程通知 渲染引擎webkit/blink： 解析html css DOM树 CSSOM树 布局树 绘制 （遇到js 通知 v8引擎执行js）
主线程通知 合成线程 去 生成像素信息 


界面分为左中右 3部分 ，同过js 实习 优先渲染 中间 左边 右边 ？？？？ nextTick？
 先把中间需要的数据处理好渲染一次 在处理左边的数据在渲染一次 在处理右边的数据在渲染 按需渲染 先渲染重要区域。



打开浏览器 - 关闭浏览器：
    进程 内存空间 线程 渲染主线程  事件循环 同步任务 异步任务 微任务 宏任务 消息队列 微任务队列 
    网络进程 域名 
    webkit/blink渲染引擎 重排重绘 DOM树 CSSOM树 布局树 分层 堆叠上下文 包含块 
    v8引擎  AST抽象语法树 词法分析 语法分析 语义分析 GC垃圾回收机制 标记清除算法 引用计数 svaweiji算法  执行上下文 
    词法环境 执行栈 作用域作用域链 闭包 


业务代码（vue ts） -  生产代码（js css html）：
包：
工具链 eslint babel prettier 
构建器 rollup webpack esbuild treeShaking 代码分割 devServer  
脚手架 vite 
框架 vue