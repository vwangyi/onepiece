# 包 package

## **包是什么**
- 通过npm init 生成package.json的文件夹就是一个包  <br />
## **package.json是什么**
- package.json是包的描述文件，里面描述了包的信息。比如包名 包版本，包依赖。



 
```txt 
作用域包就是 以@xxx/开头的包 比如 @vue/cli
非作用域包就是 不以@xxx/开头的包
公共包是开源的，所有人都可以使用
私有包是闭源的，特定的人可以使用

``` 
 