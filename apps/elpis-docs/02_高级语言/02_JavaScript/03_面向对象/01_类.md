# class

```js
// https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Reference/Classes


/* 
// 类是什么：类是封装数据和操作数据的方法。（类包含2部分 属性和方法）
// 属性：私有属性 实例属性 静态私有属性 和 静态属性
// js 的类中属性有哪些 比如 私有属性 实例属性 静态私有属性 和 静态属性 还有补充的吗

// super是什么: super是一个函数对象 可以调用 也可以用.点访问成员


类 对象 js通过原型链实现 面向对象
类：类是 封装数据(属性) 和 操作数据的方法 （类 = 属性 + 方法）
继承：
super： super是一个函数对象 可以直接调用 也可以用.访问成员 


属性（数据 字段 ）： 只读属性 可读写属性 静态属性 实例属性 私有属性
方法（函数 ）： 实例方法 静态方法 普通函数、异步函数、生成器函数或异步生成器函数


可以从以下三个方面表述一个类元素的特征：

种类：getter方法、setter方法、方法、字段
位置：静态上 或 实例上
可见性：公有 或 私有


私有： 有#前缀都是私有 
  私有字段 私有方法
  私有静态字段
  私有静态方法
  私有 getter >>> get #xixi () {}
  私有 setter >>> set #xixi () {}
  私有静态 getter >>> static getMethod(x) { return x.#method } 
  私有静态 setter >>> static #privateStaticMethod
公有：
  没有#前缀都是公有 
静态
  静态属性 static
  静态方法 static

*/


// 如何创建一个类
 
class Animal {
	// 实例属性 
	height = undefined;
	width;
	// 私有属性 
	#height = 0;
	#width; 
	// 静态属性
	static displayName = "Point"; 
	// 静态私有属性
	static #displayName = "Point1";
	
	// 静态方法 
	static distance(a, b) { 
	    return Math.hypot(dx, dy);
	} 
	// 实例方法 
	constructor(height, width) { 
		this.height = height;
		this.width = width;
	}
}
// 创建一个子类
class Person extends Animal {
	color
	constructor(height, width, color) {
		// MDN：super() 只能在 constructor 中使用，并且必须在使用 this 关键字之前调用。
	    super(height, width); // 调用父类 类似 new Animal() 
	    this.color = color;
	}
	speak() {
		super.speak();
		console.log(`${this.name} 吼叫。`);
	}
}

// 创建一个Person类 类可以实例化为对象 
class Person {
	// 实例的属性
	xx = 1; // 直接初始化 相当于 在 constructor 里 写 this.xx = 1
	xc; // 通过实例方法constructor中 通过this.xc = xx 初始化
	


	// 实例的方法 通过实例调用 内部this就是指向实例 
	// 默认的实例方法 不写也有； constructor() {}
	// new调用类 时 会调用 constructor 所以constructor一般用于初始化
	// 实例方法 就是 es5的 挂在 prototype上的 原型方法
	constructor(x) {
		this.xc = x; 
		this._abc = x; // 私有属性
	}
	xxxxx() {} // 实例方法

	// 不建议在类外  类名.xx = function () {} 
	static xxx() {} // 静态方法 通过类名调用。比如 Array.isArray() 
    static NAME = '嘿嘿'  // 静态属性 通过类名调用 比如  Math.PI

	// 私有属性 命名_开头  配合getter setter 使用
	_aaa;
    get aaa() { return this._aaa }
    set aaa(val) { this._aaa = val }
	// 私有方法 命名_开头
	_xxxx() {}
	
}
类的继承
// 继承之后 才确定父子关系例化对象 调用一个方法 先看自身有没有 再看父类有没有

// super 
//     // super关键字
//     // super可以调用父类的 constructor函数 并传参  也可以调用父类的普通函数
//     // super必须在 子类用this之前调用 
//     super()  // 在子类的constructor中  调用父类constructor函数
//     super.父类方法名()   // 在子类的普通函数中  调用父类方法

//     子类和父类有同名方法时
//     子类用 super.名字()  调用父类的方法  用this.名字() 调用自己子类的方法
//     如果没用重名方法 super.名字() 和 this.名字()  没有区别 因为只能找到一个方法

// 如何操作数据 Class类
// // 类本身没用 只有实例化为对象才有用
// const p = new Person('王', 18) // 创建一个对象

// p.xx 
// p.xx = xx 
// p.xxx() 
// Person.NAME 

// Class类

// 
class Rectangle {
  constructor(height, width) {
    this.name = "矩形";
    this.height = height;
    this.width = width;
  }
}

class FilledRectangle extends Rectangle {
  constructor(height, width, color) {
    super(height, width);
    this.name = "填充矩形";
    this.color = color;
  }
}


class Person {
    static NAME = 1
    NAME = 3
    



    // 实例属性
    xx = 1; // js可以直接初始化  类内部通过this.xx访问  类外部通过实例对象.xx访问
    xx; // 也可以不初始化 在constructor里初始化 类内部通过this.xx访问  类外部通过实例对象.xx访问
 

    // 实例方法 内部this指向实例对象
    constructor() {} // 默认构造函数不写也有 类外部调用是通过 把类实例化为对象时 new Xxxx(); 调用constructor  一般用于初始化
    constructor(x) {
        this.xx = x
    }
    xxx() {} // 内部调用 通过this.xxx()  类外部调用 通过实例对象.xxx()
    xxx() {}
    _xxx() {} // 私有方法

    // 静态方法 内部this指向类本身 因为是通过 类名.xxx() 调用
    static xxx() {} // 类外部调用 通过 类名.xxx() 推荐    也可以通过实例对象.xxx() 不推荐
    static xxx() {}

    // 私有属性 注意要搭配对于的 get set方法
    _a; 
    get a() { return this._a }
    set a(val) { this._a = val }
}
class Person { 
    
    // 实例方法
    // constructor是类的默认的构造函数  new的时候就初始化调用一次  作用是创建实例对象 
    constructor(形参) {
        this // 实例对象
        // 实例属性
        this.xx = 形参
    }  
    // 实例方法
    xixi() {
        console.log(this) // this指向实例对象
    }
    // 实例属性 
    haha = 123; // 也可以不赋值


    // 静态方法 添加static关键字 就把方法提升到类层面 实现所有实例对象共享
    static hello() {
        console.log(this) // this指向类本身
    }

    // 静态属性
    static NAME = '嘿嘿'  // 我是静态属性 需要 Person.NAME访问  Math.PI
}







  class Promise { 
    /**
     * 1. 类的内部直接定义的方法是原型方法 constructor是默认的原型方法
     *    let 实例 = new Promise() 会自动调用 constructor 相当于执行了 实例.constructor()
     * 2. 原型方法的内部 this是实例对象 换言之 原型方法的内部可以拿到实例对象
     * 3. 原型方法的内部 定义的属性是私有属性 命名以_开头 const _this = this
     * 4. 原型方法的内部 定义的方法是私有方法 function resolve() {}
     * 5. 私有属性和私有方法 只能在类或构造函数的内部使用
     * 
     * 挂在实例this上的方法 叫实例方法 会造成内存浪费
     * 挂在实例this上的属性 叫实例属性 
     * 
     * 
     */
 
    constructor() {  
      this.PromiseState = 'pending'
      this.PromiseResult = null

      function resolve() { }
      function reject() { }
    } 
    then() {  `this都是实例对象`  }
    catch() { `this都是实例对象`  } 


    /**
     * 1. 类的内部用static定义的方法是静态方法 static all() {}  
     *    挂在类或构造函数上 用Promise.all()访问
     * 
     * 2. 类的内部 用 static get 定义的属性是静态属性 static get flag() { return 'hello' } 
     *    挂在类或构造函数上 用Promise.flag访问
     * 
     */
 

    // 静态方法 挂在类或构造函数上 Promise.all()
    static all() {}
    // 静态属性 挂在类或构造函数上 Promise.flag
    static get flag() { return 'hello' }


    /**
     * 1. 直接在类的内部 给未定义的属性赋值 会自动变成实例属性 
     *    a = 1 相当于 在 constructor 里 写 this.a = 1
     */ 
    a = 1  
  }



  // 静态属性 静态方法 (不用)
//   Promise.AGE = 20
//   Promise.all = function() {}


// 实例对象.hasOwnProperty('a')   // 返回布尔 参数a方法是不是 实例对象上的实例方法
// 访问类的静态方法 
// 1 通过 类名 访问  Promise.all()
// 2 通过实例的constructor (实例.constructor属性 指向 类) 访问 静态方法 
// new Promise().constructor.静态方法() 



// 构造函数 es5

// 构造函数
// function Start() {
//   if(new.target === void 0) {
//     throw new Error('只能new调用')
//   }

//   // const a = 1
//   // const updateA = (newValue) => {
//   //   this.a = newValue 
//   // }
// }
// new Start()



// es6类   5分
//  es6的类  是 es5构造函数的语法糖
// JavaScript是面向对象的编程语言

// 泛指 => 特指
// 类  =>  实例对象
// 手机 => 我的手机
// 面包 =>  我今早吃的面包
// 数组 => const arr = []
// 对象 => const obj = {}

// 类本身没啥用  把类实例化为对象  对象才有用
// 面向对象
// 面向对象 是 以对象的功能来划分问题 不是步骤

// 面向对象的三大特性: 封装 继承 多态

// 面向过程的性能 比 面向对象好一点

类
// 类名是大驼峰

class Arr {
    // 类的构造函数 默认方法  new的时候就调用 constructor 一次  作用是创建实例对象
    constructor(形参) {
        // 实例属性
        this.xx = 形参
    }
    属性名 = 属性值
    方法名() {
        // 随便做某事
    }

    // 静态方法
    static haha() {
    }
    // 静态属性
    static NAME = '嘿嘿'  // 我是静态属性 需要 Person.NAME访问  Math.PI
}

// 把类实例化为对象
const obj = new Person(实参)
类的继承
// 继承之后 才确定父子关系
    class Animal {
        skin = '毛';
        hasEye = true;
        walk() {
            console.log('溜达')
        }
    }
    class Dog extends Animal {
        // skin = '毛';
        // hasEye = true;
        // walk() {
        //     console.log('溜达')
        // }
        sayHi() {
            console.log('汪汪')
        }
    }

    const dog = new Dog()
    dog.walk()   // '溜达'



    // super和this   

// super表示父类 也叫超类 

// 抽象类 abstract  只能继承 不能new 

    // 继承中 属性或方法的查找原则: 就近原则
    // 如果子类 实例化对象 调用一个方法 先看自身有没有 再看父类有没有

// super关键字
//     // super关键字
//     // super可以调用父类的 constructor函数 并传参  也可以调用父类的普通函数
//     // super必须在 子类用this之前调用 
//     super()  // 在子类的constructor中  调用父类constructor函数
//     super.父类方法名()   // 在子类的普通函数中  调用父类方法

//     子类和父类有同名方法时
//     子类用 super.名字()  调用父类的方法  用this.名字() 调用自己子类的方法
//     如果没用重名方法 super.名字() 和 this.名字()  没有区别 因为只能找到一个方法

// 封装 类和构造函数

// // 构造函数的方法 = Object.keys()  = 静态方法  =   类的方法 

// 类的静态方法和属性  static haha() { }   static NAME = 'xx'  
// 实例对象的方法和属性 this.haha = function () { }   this.name = 'xxx'


// 构造函数的方法  只能通过构造函数访问    Object.keys()
// 构造函数的prototype上的方法  挂在原型上 所有实例对象 都可以访问  [1,2,3,4].map()



```
# 构造函数

```js
function Array1() {
    Object.call(this);

    this.length = 0;

    
}

Array1.prototype = Object.create(Object.prototype);
Array1.prototype.constructor = Array1;

```