# 正则

```js


/* 创建正则的数据方式 */
const reg1 = /[a-zA-Z0-9_-]+@[a-zA-Z0-9_-]+(\.[a-zA-Z0-9_-]+)+/
const reg2 = new RegExp('^(?:\+86)?1[3-9]\d{9}$');
var 变量名 = new RegExp(/表达式/);
var regexp = new RegExp(/123/);   // new 构造函数 创建
var regexp1 = /123/;              // 字面量方式创建

reg1.test(value)  
reg.exec(str)









// +++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
正则特殊字符
特殊字符(元字符)
边界符
边界符(位置符)
^开头     $结尾 
/abc/.test('aabcgg')  //true   只要包含 'abc' 都符合
/^abc/.test('abcgg')  //true   只要 以 'abc' 开头 都符合
/abc$/.test('dddabc')   //true    只要 以 'abc' 结尾 都符合 
/^abc$/.test('abcabc')  //false   如果^和$在一起使用 必须是精确匹配 只能是abc

字符类
[ ]  可选
字符类表示有一系列字符可供选择，只要匹配其中一个就可以了。
所有可供选择的字符都放在方括号内。
/[abc]/.test('andy')  //true    只要包含abc中的一个字母 都符合 
/[abc]/.test('red')  //false    
/^[abc]$/.test(aa);  //false   只能是abc其中一个字母     注意只要有^$ 那就代表 只能是
[ - ]   中括号内部的范围符 - 
/^[a-z]$/.test('a');  //false   只能是26小写字母中的一个
字符组合
/^[a-zA-Z]$/.test('a');  //false   只能是52个字母(a-z和A-Z)中的一个
/^[a-zA-Z0-9-_]$/.test('-');  //false   只能是 (a-z和A-Z和0-9和-和_)中的一个
[ ^ ]   中括号里面的 ^  代表取反        跟外部的^ 完全不一样
/^[^a-zA-Z0-9-_]$/.test('!'); //true 只能是 除去(a-z和A-Z和0-9和-和_)以外的范围中的一个
量词符
量词符 用来规定某个模式出现的次数
 *  重复0次或更多次    >=0
+  重复1次或更多次      >=1
?  重复0次或更1次        1 || 0
{n}   重复n次              重复3次
{n,}   重复n次或更多次       重复 >=3次
{n,m}   重复n到m次
/^a*$/.test('')   //true        量词* 相对于  重复>=0 次
/^a*$/.test('aaaaa')   //true
/^a+$/.test('')   //false       量词+ 相对于  重复>=1  次
/^a+$/.test('aaaaa')   //true
/^a?$/.test('')   //true         量词? 相对于  重复 0或1 次
/^a?$/.test('a')   //true

/^a{3}$/.test('aaa')  // true     加了量词{3}   表示 量词前面的模式 可以出现3次
/^a{3}$/.test('a')   // false        
/^a{3,}$/.test('aaa') // true     加了量词{3,}  表示 量词前面的模式 可以出现>=3次
/^a{3,}$/.test('a')   // false    
/^a{3,6}$/.test('aaa')  // true  加了量词{3,6} 表示 量词前面的模式 可以出现(大于等于3 小于等于6)次
/^a{3,6}$/.test('a')   // false    

/^abc{3}$/.test(abccc) //true        只表示让c出现3次
/^(abc){3}$/.test(abccc) //true      表示让abc出现3次
{ }  表示量词
[ ]  表示可选  字符集合
( )  表示优先级

能够说出正则表达式的作用
能够写出简单的正则表达式
能够使用正则表达式
对表单进行验证
能够使用正则表达式替换内容
.
正则表达式概述
正则表达式在JavaScript中的使用
正则表达式中的特殊字符


正则表达式中的替换

正则表达式(Regular Expression )是用于匹配字符串中字符组合的模式。在JavaScript中，正则表达式也是对象。


正则表通常被用来检索、替换那些符合某个模式（规则)的文本，
例如
● 验证表单∶用户名表单只能输入英文字母、数字或者下划线，昵称输入框中可以输入中文(匹配)。
● 正则表达式还常用于过滤掉页面内容中的一些敏感词(替换)，
● 或从字符串中获取我们想要的特定部分(提取)等。
正则表达式特点：
1.灵活性、逻辑性和功能性非常的强。
2.可以迅速地用极简单的方式达到字符串的复杂控制。
3.对于刚接触的人来说，比较晦涩难懂。比如:^\w+([-+.J]w+)*@\w+([-J]\w+)L.w+(I-J]w+)$
4.实际开发,一般都是直接复制写好的正则表达式.但是要求会使用正则表达式并且根据实际情况修改正则表达式. 比如用户名: /^[a-z0-9_-][3,16}$/

用户名验证
 /^[a-zA-Z0-9_-]$/.test('andy')         // false   这是多选1   默认只能出现1次
 /^[a-zA-Z0-9_-]{2,8}$/.test('andy-red')    // true 后面加了量词 {2,8}  可以出现2到8次
功能需求:
1.如果用户名输入合法，则后面提示信息为：用户名合法，并且颜色为绿色
2.如果用户名输入不合法，则后面提示信息为：用户名不符合规范，并且颜色为绿色



预定义类
预定义类 指 某些常见模式 的简写方式
\d        [0-9]
\D        [^0-9]
\w        [A-Za-z0-9]
\W      [^A-Za-z0-9]
\s         [\t\r\n\v\f]    匹配空格(包括回车 tab 空格等)
\S         [^\t\r\n\v\f]
/^\d$/   等于  /^[0-9]$/

座机号码验证
座机号码验证: 全国座机号码两种格式: 010-12345678 或者 0530-1234567
正则里面(或者符号) 是  |   
var rg = /^\d{3}-\d{8}|\d{4}-\d{7}$/;     //010-12345678   0530-1234567
var rg = /^\d{3,4}-\d{7,8}$/;            //010-12345678   0530-1234567  
正则的替换
replace 替换
replace()方法 可以实替换字符串操作，用来替换的参数可以是一个字符串或正则表达式
stringObject.replace(regexp/substr,replacement)
● 参数1 是被替换的字符串或正则
● 参数2 是替换为字符串
● 返回是一个替换完毕的新字符串

正则表达式参数
/正则表达式/[switch]
switch 也称修饰符 按照什么样的模式来匹配，有三种值：

    g:全局匹配          /正则表达式/g     global全局
    i:忽略大小写         /正则表达式/i    
    gi:全局匹配+忽略大小写    /正则表达式/gi
<body>
    <textarea name="" id="message"></textarea>
    <button>提交</button>
    <div></div>
    <script>
        var text = document.querySelector('textarea');
        var btn = document.querySelector('button');
        var div = document.querySelector('div');
        btn.onclick = function () {
            div.innerHTML = text.value.replace(/激情|gay/g, "**");
        }
    </script>
</body>


正则表达式
正则表达式: 描述一个规则 用来匹配字符串
在前端js中 能不用正则就不用  因为性能不好 容易卡
 /^[a-zA-Z0-9_]{6,10}$/.test('') //   6到10位 字母和数字和下划线组成的
RegExp.prototype.test()
/^1[3-9]\d{9}$/.test('13912341234')    // true 
// 返回true false

RegExp.prototype.exec()
/^1[3-9]\d{9}$/.test('13912341234')    // true 
// 返回 符合正则的字符串 组成的数组

字符串关于正则方法
String.prototype.match()
str.match(正则)   // 返回匹配的字符串数组项 组成的数组







正则表达式
正则用于匹配字符串中 字符组合的模式   //是js语法 中间才是正则表达式  正则表达式 所有语言通用
● 正则表达式 （Regular Expression）:   是用于匹配字符串中字符组合的模式。
● 在JavaScript中，正则表达式也是对象。
正则创建
● 创建正则对象
var 变量名 = new RegExp(/表达式/);
var regexp = new RegExp(/123/);   // new 构造函数 创建
var regexp1 = /123/;              // 字面量方式创建

● 测试参数是否符合正则
正则对象.test()    测试参数是否符合正则
/123/.test(123)     // true
/123/.test('abc')   // false
正则特殊字符
特殊字符(元字符)
边界符
边界符(位置符)
^开头     $结尾 
/abc/.test('aabcgg')  //true   只要包含 'abc' 都符合
/^abc/.test('abcgg')  //true   只要 以 'abc' 开头 都符合
/abc$/.test('dddabc')   //true    只要 以 'abc' 结尾 都符合 
/^abc$/.test('abcabc')  //false   如果^和$在一起使用 必须是精确匹配 只能是abc

字符类
[ ]  可选
字符类表示有一系列字符可供选择，只要匹配其中一个就可以了。
所有可供选择的字符都放在方括号内。
/[abc]/.test('andy')  //true    只要包含abc中的一个字母 都符合 
/[abc]/.test('red')  //false    
/^[abc]$/.test(aa);  //false   只能是abc其中一个字母     注意只要有^$ 那就代表 只能是
[ - ]   中括号内部的范围符 - 
/^[a-z]$/.test('a');  //false   只能是所有小写字母中的一个
字符组合
/^[a-zA-Z]$/.test('a');  //false   只能是52个字母(a-z和A-Z)中的一个
/^[a-zA-Z0-9-_]$/.test('-');  //false   只能是 (a-z和A-Z和0-9和-和_)中的一个
[ ^ ]   中括号里面的 ^  代表取反        跟外部的^ 完全不一样
/^[^a-zA-Z0-9-_]$/.test('!'); //true 只能是 除去(a-z和A-Z和0-9和-和_)以外的范围中的一个
量词符
量词符 用来规定某个模式出现的次数
 *  重复0次或更多次    >=0
+  重复1次或更多次      >=1
?  重复0次或更1次        1 || 0
{n}   重复n次              重复3次
{n,}   重复n次或更多次       重复 >=3次
{n,m}   重复n到m次
/^a*$/.test('')   //true        量词* 相对于  重复>=0 次
/^a*$/.test('aaaaa')   //true
/^a+$/.test('')   //false       量词+ 相对于  重复>=1  次
/^a+$/.test('aaaaa')   //true
/^a?$/.test('')   //true         量词? 相对于  重复 0或1 次
/^a?$/.test('a')   //true

/^a{3}$/.test('aaa')  // true     加了量词{3}   表示 量词前面的模式 可以出现3次
/^a{3}$/.test('a')   // false        
/^a{3,}$/.test('aaa') // true     加了量词{3,}  表示 量词前面的模式 可以出现>=3次
/^a{3,}$/.test('a')   // false    
/^a{3,6}$/.test('aaa')  // true  加了量词{3,6} 表示 量词前面的模式 可以出现(大于等于3 小于等于6)次
/^a{3,6}$/.test('a')   // false    

/^abc{3}$/.test(abccc) //true        只表示让c出现3次
/^(abc){3}$/.test(abccc) //true      表示让abc出现3次
{ }  表示量词
[ ]  表示可选  字符集合
( )  表示优先级

能够说出正则表达式的作用
能够写出简单的正则表达式
能够使用正则表达式
对表单进行验证
能够使用正则表达式替换内容
.
正则表达式概述
正则表达式在JavaScript中的使用
正则表达式中的特殊字符


正则表达式中的替换

正则表达式(Regular Expression )是用于匹配字符串中字符组合的模式。在JavaScript中，正则表达式也是对象。


正则表通常被用来检索、替换那些符合某个模式（规则)的文本，
例如
● 验证表单∶用户名表单只能输入英文字母、数字或者下划线，昵称输入框中可以输入中文(匹配)。
● 正则表达式还常用于过滤掉页面内容中的一些敏感词(替换)，
● 或从字符串中获取我们想要的特定部分(提取)等。
正则表达式特点：
1.灵活性、逻辑性和功能性非常的强。
2.可以迅速地用极简单的方式达到字符串的复杂控制。
3.对于刚接触的人来说，比较晦涩难懂。比如:^\w+([-+.J]w+)*@\w+([-J]\w+)L.w+(I-J]w+)$
4.实际开发,一般都是直接复制写好的正则表达式.但是要求会使用正则表达式并且根据实际情况修改正则表达式. 比如用户名: /^[a-z0-9_-][3,16}$/

用户名验证
 /^[a-zA-Z0-9_-]$/.test('andy')         // false   这是多选1   默认只能出现1次
 /^[a-zA-Z0-9_-]{2,8}$/.test('andy-red')    // true 后面加了量词 {2,8}  可以出现2到8次
功能需求:
1.如果用户名输入合法，则后面提示信息为：用户名合法，并且颜色为绿色
2.如果用户名输入不合法，则后面提示信息为：用户名不符合规范，并且颜色为绿色



预定义类
预定义类 指 某些常见模式 的简写方式
\d        [0-9]
\D        [^0-9]
\w        [A-Za-z0-9]
\W      [^A-Za-z0-9]
\s         [\t\r\n\v\f]    匹配空格(包括回车 tab 空格等)
\S         [^\t\r\n\v\f]
/^\d$/   等于  /^[0-9]$/

座机号码验证
座机号码验证: 全国座机号码两种格式: 010-12345678 或者 0530-1234567
正则里面(或者符号) 是  |   
var rg = /^\d{3}-\d{8}|\d{4}-\d{7}$/;     // 010-12345678   0530-1234567
var rg = /^\d{3,4}-\d{7,8}$/;             // 010-12345678   0530-1234567  
正则的替换
replace 替换
replace()方法 可以实替换字符串操作，用来替换的参数可以是一个字符串或正则表达式
stringObject.replace(regexp/substr,replacement)
● 参数1 是被替换的字符串或正则
● 参数2 是替换为字符串
● 返回是一个替换完毕的新字符串

正则表达式参数
/正则表达式/[switch]
switch 也称修饰符 按照什么样的模式来匹配，有三种值：

    g:全局匹配          /正则表达式/g     global全局
    i:忽略大小写         /正则表达式/i    
    gi:全局匹配+忽略大小写    /正则表达式/gi
<body>
    <textarea name="" id="message"></textarea>
    <button>提交</button>
    <div></div>
    <script>
        var text = document.querySelector('textarea');
        var btn = document.querySelector('button');
        var div = document.querySelector('div');
        btn.onclick = function () {
            div.innerHTML = text.value.replace(/激情|gay/g, "**");
        }
    </script>
</body>

```