+ 前置知识 视口



方案一 媒体查询

方案二 容器查询 grid flex clamp()函数





没有设置 meta viewport 标签 默认 缩放到 980px

设置了 meta viewport 标签 就缩放到 设置的值。





<!-- 这是一张图片，ocr 内容为：布局视口和视觉视口 CODERWHY 布局视口(LAYOUTVIEWPORT) 默认情况下,一个在PC端的网页在移动端会如何显示呢? 口第一,它会按照宽度为980PX来布局一个页面的盒子和内容; 口第二,为了显示可以完整的显示在页面中,对整个页面进行缩小; 我们相对于980PX布局的这个视口,称之为布局视口(LAYOUT VIEWPORT); 布局视口的默认宽度是980PX; 视觉视口(VISUALVIEWPORT) 右侧有一部分区域 如果默认情况下,我们按照980PX显示内容 手机端浏览器会默认对页面进行缩放以显示到用 就会无法显示, 户的可见区域中 那么显示在可见区域的这个视中,就是视觉视觉视口(VISUALVIEWPORT). 在CHROME上按SHIFT+鼠标左键可以进行缩放. -->
![](https://cdn.nlark.com/yuque/0/2025/png/34696752/1766632279918-a1e13086-6f4e-4f6a-b5b6-7d00d6c4078f.png)

