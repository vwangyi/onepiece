# 增

services层 是站在数据库角度 把数据库的操作封装方法给 controller业务层调用

store层是 站在前端渲染数据结构的角度 把渲染数据的操作封装方法 给 view层页面层调用


### insert 增
```sql

insert into 
t_user(`字段名1`, `字段名2`) -- 列名如果是关键字 必须用反引号包裹  任何情况下都不能用单引号或双引号包裹
values('值1', '值2'), ('值11', '值22') -- 可以插入多行     值不能用反引包裹 只能用单引号或双引号包裹
```
