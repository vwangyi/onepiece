
# 对数
```text 
logₐ(b) = c;

log是对数符号
a是对数的底数 （底数必须大于0且≠1）
b是对数的真数 （真数必须大于0）
c是对数值本身

对数 必须要有 底数和真数 就像 除法 必须要有 被除数和除数 
 
对数是指数的逆运算
```

## 举例
```text
log₂(8) = 3

2是底数，8是真数，3是对数值
 
"以谁为底，取谁的对数，得谁的对数"
读作：
log以2为底，取真数8的对数，是3
log以2为底，8的对数，是3
``` 
  


