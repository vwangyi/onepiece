# 包环境版本锁定
```json
// package.json
"engines": {
  "node": ">=22.14.0",
  "npm": ">=10.9.2",
  "pnpm": ">=10.15.1"
}
```

```yaml
# .npmrc
engine-strict=false # false表示 警告程度 默认值 也就是说不用这个文件也就是这个值

engine-strict=true # true表示 报错程度
```