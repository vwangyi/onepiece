# 路径别名

> webpack.config.js

```js
module.exports = {
    resolve: {
        alias: {
            '@', './src',
            '@', './src',
        },
        extendstions: ['.js', '.vue', '.less'], // 可以省略文件后缀
    },
}
```
