## 安装TS环境
全局安装 typescript 得到 tsc命令， tsc --init 可以生成 tsconfig.json配置文件
 
 
## @types
- @types是 ts官方的类型库 包含了很多对js代码的类型描述。
- 比如 jquery是纯js写的 也可以在ts中使用jquery， 安装ts官方提供的 @types/jquery
- @types/node   pnpm i -D @types/node

