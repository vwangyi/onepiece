

# 打包入口
```js
module.exports = {
    entry: './src/main.js', // 相对于启动目录的一个相对路径 
    output: {
        path: path.resolve(__dirname, './dist'),
        filename: 'js/[name]_[chunkhash:8].bundle.js',
    }
}
```