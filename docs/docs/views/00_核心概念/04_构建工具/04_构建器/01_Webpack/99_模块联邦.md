# Module Federation 模块联邦

- https://webpack.docschina.org/concepts/module-federation/
 
- 微前端


模块联邦就是两个独立项目可以交换模块加载
webpack是把业务文件打包成产物文件的构建工具。
webpack的原理是xxx
babel是一个js编译器，可以把新语法转为旧语法。比如 箭头函数转普通函数，好处是写的是新语法但可以兼容旧浏览器。
babel原理是什么
html css scss
js ts 
vue
webpack （nodejs）

npm run serve:dev
npm run build:dev
npm run serve:prod
npm run build:prod
入口 entry  spa项目只有一个入口。mpa项目多个入口
代码分割 + 按需加载

请求次数应该越少越好 所以代码分割
css-loader就是把css文件源码字符串转为js文件
图片是一个二进制buffer而不是string
loader
转换源码字符串
compiler对象和 completion对象区别
compiler只有一个 后者可能有多个
无非就是监听事件 作处理 。处理是同步就用tap 处理是异步就用tapPromise
process.cwd（）当前执行命令路径
__dirname 当前文件路径

copy-webpack-plugin
html-webpack-plugin
clean-webpack-plugin
webpack-dev-server 既不是loader也不是plugin

file-loader
url- loader

webpack内置插件
defineplugin
bannerplugin
providerplugin
多次导入 只有第一次走loader 后续都走内存
移动端 经常用 css in js
css muddle 尽量写一个类名 而不要写父子后代选择器
解决css类名冲突 类名规范BEM cssinjs css module

解决重复样式：2个方案 cssinjs 预编译器scss

bem命名规范
scss预编译器
css in js
css module
post css
前端方案繁多 没有最佳实践 那么程序员的作用就是熟悉公司业务后 根据公司业务定制一套最佳实践方案 也就是方案选型

bem命名规范
scss预编译器
css in js
css module
post css后处理器

修改loader匹配范围
写loader或plugin 实现 公司组件库不用引入 直接使用
浏览器没有刷新也就不会请求所有数据 而是服务器主动把只有改变的数据发送给客户端 

解决项目的问题 hmr没有实时更新
分包策略

网络传输 主要是生产环境  开发环境是 本机到本机
动态依赖 当依赖路径不确定 至少给一个文件夹前缀 webpack会把文件夹下所有东西都打包到结果产物中
gzip压缩算法 压缩不是简单移除空白符 而是转为二进制 且相同字符进行复用

