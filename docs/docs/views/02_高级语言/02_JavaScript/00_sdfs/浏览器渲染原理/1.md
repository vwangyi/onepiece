```js
function render(html) {
    return new Promise((resolve, reject) => {
        // 1. 解析html字符串
        const dom = parseHTML(html)
        // 2. 解析css字符串
        const cssStr = parseCssStr()
        // 3. 布局
        const layout = layout()
        // 4. 分层
        const layers = layers()
        // 5. 图层绘制
        const pixels = paint(layers)
        // 6. 栅格化 分块
        resolve(pixels)
    })

}
// https://github.com/chromium/chromium chromium 浏览器源码
function render(htmlStr) {
     /**
     * 每个过程都有输入和输出 这次的输出就是下次的输入
     */

      // 1 parseHTML 解析html字符串   一颗DOM数 也就是一个DOM对象  无限递归的过程 parseHTML函数返回的就是这样的dom对象

        // const dom = {
        //     ....
        //     children: {
        //         ....
        //         children: {
        //             ...
        //     }
        // }
        // 为什么要把html字符串转换成dom对象 因为dom对象更加容易操作 字符串不方便增删改查操作  就是数据处理 C++写的

        // 把html字符串里面的 html标签部分 转换成dom对象
    const dom = parseHTML(htmlStr)

    // 2 parseCssStr 解析css字符串 样式计算
    // 把html字符串里面的 css部分 转换成css对象  返回的也是一个对象
    // const styleSheetList = {
    //     // 选择器
    //     '.a': {
    //         // 样式
    //         color: 'red',
    //         fontSize: '20px'
    //     },
    //    body: {
    //        color: 'black',
    //        fontSize: '14px'
    //    }
    //     '.b': {
    //         color: 'blue',
    //         fontSize: '30px'
    //     }
    // }
    // 内部样式表 外部样式表  行内样式表  浏览器默认样式表 4种表 计算出来的样式表  link可以多个  style可以多个  行内样式只有一个
    // js除了浏览器的默认样式表 不能操作 其他样式表都可以操作 而C++可以操作所有的样式表
    const CSSObjectModel = parseCssStr(htmlStr)  // CSSObjectModel  cssom
    // 3 layout 布局
    const layout = layout()
    // 4 分层
    const layers = layers()
    // 5 图层绘制
    const pixels = paint(layers)
    // 6 栅格化 分块
    return pixels
}


// 渲染主线程
// 预解析线程
// 网络线程
// 延迟加载线程



// 1. 解析html字符串 得到 dom 和 cssom
function parseHTML(HTML) {
    const result = {}
    // c++生成两颗树 一个是dom树 一个是cssom树  是对象结构 方便js操作 但浏览器的默认样式表 js不能操作

    // 当渲染主线程 遇到 link标签 或 style标签 渲染主线程 交给 预解析线程 预解析线程快速浏览之后发现需要下载 就交给网络线程 网络线程下载完css 交给预解析线程解析 预解析线程解析css后交给渲染主线程   （和defer async ）
    // 当渲染主线程 遇到 script标签  js时候 必须暂停一切行为 等待js执行完毕后才能继续解析html  也就是说js是阻塞的html的解析 （js有可能会改动已经解析过的DOM 所以必须等js执行完毕后才能继续解析html）
    // DOM是一边解析 一边生成的 不是等全部解析完毕后再生成
    return result
}

// 2. 样式计算  得到 有样式的dom
function recalculateStyle(dom, cssom) {
    const computedStyle = {}
    // css属性值的计算过程 问dy 第二节课资料
    // 视觉格式化模型
    // 盒模型 包含块
    return computedStyle // 得到计算后的样式
}
// 3. 布局
function layout(dom) {
    const layout = {
        // 位置  位置相对的是 包含块的父亲  问dy 第三节课资料
        // 大小
        // 边距
        // 边框
        // 内边距
        // 盒模型
    }
    return layout
    // 得到布局树 也就是 layout树 布局后的DOM 和DOM树有点区别 但是其本质都是 数据描述信息
    // 没有几何信息就不会生成到layout树上  display:none不会生成到layout树上
    //  (::before ::after)在dom树上不存在 但在layout树上存在

    // 行盒 块盒 是css决定的 而html只是提供了语义化 （行级元素块级元素说法不标准）
    // 内容必须放在行盒中 行盒和块盒不能相邻

    // 有很多情况都会导致 dom树和layout树不一样  比如 display:none  visibility:hidden  position:absolute  float:left  overflow:hidden  等等
}
// 4. 分层  类似ps的图层 （z-index等  跟堆叠上下文相关的属性）  css属性 will-change 3d变换 透明度变化 会触发硬件加速 会生成新的图层
function layers(layout) {
    const layers = {}
    return layers
}

// 5. 图层绘制
function paint(layers) {
    // 生成绘制的指令： 比如
    // 指令1： 将画笔移动到 10,30 的位置
    // 指令2： 画一个200,300的矩形
    // 指令3： 用红色填充矩形
    // 指令4： 用蓝色描边矩形
    // 有点像canvas的绘制指令 canvas就是一种浏览器开放出来的绘制api

    // 渲染主线程 会把这些指令交给GPU 渲染主线程工作到此为止  GPU会把这些指令转换成硬件指令 交给显卡  显卡会把这些指令转换成电信号 交给显示器  显示器会把电信号转换成光信号  也就是我们看到的图像
    const pixels = {}
    return pixels
}

// 6. 分块 (这是合成线程做的 不是渲染主线程了   渲染线程有 渲染主线程和合成线程 还有其他线程)
function tiling(pixels) {
    // 把上一次的很多层 分块 分为很多小块
    // 为什么要分块？ 因为分块后可以实现增量渲染 也就是说只渲染那些变化的部分  不需要重新渲染整个页面
    // 分块会将每一层分为多个小的区域
}

// 7. raster 光栅化
function raster() {
    // 光栅化将每个块变为位图
    // 优先处理靠近视口的块
    // 合成线程会调用 GPU 来处理光栅化 (gpu是显卡 专做显示工作的 cpu是主板 专做计算工作的  gpu是cpu的协处理器)
}
// 8. 画
function draw() {
    // 合成线程计算出每个位图在屏幕上的位置 交给GPU线程进行最终呈现 这里的GPU线程是浏览器提供的 并非系统的GPU

    对于transform属性 也是最后一步 画 这一步执行的 所以性能好
}



渲染进程里有 渲染主线程 合成线程  其他线程
渲染进程是放在沙盒里面的 与外界隔离  和操作系统硬件是隔离的 也就是浏览器比较安全 不会对系统造成影响 所以浏览器浏览网页很安全 但是你通过浏览器下载东西 就是另外一回事了 因为你可能下载了病毒
所以合成线程不能直接调用系统的GPU


```

js修改width height等几何信息 就会发生重排reflow 因为大小改变了嘛 重排也是异步任务 都要去排队执行

js修改颜色 位置等样式信息 就会发生重绘repaint 因为颜色改变了嘛

发生重排 浏览器经历的步骤为 从第二步到第8步 2计算后的样式 3布局 4 分层 5绘制 6分块 7光栅化 8画

发生重绘 浏览器经历的步骤为 从第五步到第8步 5绘制 6分块 7光栅化 8画

所以重排一定会发生重绘 重绘不一定会发生重排
所以重排一定比重绘更加耗性能
