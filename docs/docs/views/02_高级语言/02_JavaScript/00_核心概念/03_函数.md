# 函数


## 作用域/作用域链
- 作用域是指变量或函数在代码中可以被访问的范围。分为 全局作用域 函数作用域和块级作用域。
- 作用域链就是V8引擎查找变量或函数的一种机制。
- 当在一个作用域中使用变量时，V8引擎从当前作用域找，然后是外层作用域，一直找到全局作用域，找不到就报错。


## 什么是词法作用域 / 动态作用域 
- 词法作用域：函数作用域在定义时确定，函数在哪里定义。
- 动态作用域：函数作用域在调用时决定，函数在哪里调用。

## this/call/bind/apply   
- this 在全局作用域下, 指向window （不管是否严格模式）
- this 在函数作用域下，和调用方式有关
    直接调用，严格模式是 undefined 非严格模式是全局对象window
    对象调用 a.b.c.d.f();  如果把方法用变量装起来 然后调用这个变量 那就是直接调用
    new调用 实力对象  new 类的话 本质是 new 调用 类的constructor方法 
    call bind apply 调用，this指向第一个参数
- 块级作用域和箭头函数 都没有自己的this 都是外层的this  

## 手写call
- 手写call本质也是利用 原生规则  this指向调用者

## 严格模式
- 在脚本或函数的顶部写 字面量的字符串 'use strict'; 

## 箭头函数普通函数区别
this arguments prototype 都没有 

- 箭头函数没有this，箭头函数使用this是外层的this。
- 箭头函数没有 arguments，批量获取参数 只能使用es6的剩余参数 
- 箭头函数 有 `__proto__` 但没有 `prototype` ，所以不能作为构造函数也不能 new 调用  (Array.prototype.constructor )


普通函数：this 是动态绑定的，取决于调用方式
箭头函数：this 是词法绑定的，继承外层作用域的 this

普通函数：有自己的 arguments 对象
箭头函数：没有 arguments，需要使用 rest 参数

普通函数：可以用作构造函数（通过 new 调用）
箭头函数：不能用作构造函数，会报错


普通函数：可以使用 new.target 检测是否被 new 调用
箭头函数：没有 new.target

普通函数：有自己的 super 绑定
箭头函数：没有自己的 super，继承外层

(()=>{}).__proto__ === (function () {}).__proto__ // true  所以都有call 但是箭头函数调用call没效果

 

## 柯里化
- 柯里化是把多参函数优化为嵌套的单参函数 是什么 应用 柯里化 固定化参数 好处 

## 高阶函数
- 高阶函数：函数的返回值或参数也是函数
- 操作型函数：这个函数会修改外部状态外部某个变量
- 取值型函数：这个函数会返回一个值 或 传递一个回调接收参数 

 
 
 
- 工厂函数是 创建并返回实例对象的函数，好处是封装了复杂性 私有性。对于 用类创建对象和工厂函数创建对象的区别 vue也是用 createApp工厂函数来创建vue实例对象
- 
 

   
## 值传递和引用传递  
- 值传递就是 两个变量完全独立，给其中一个赋值，不影响另一个。
- 引用传递就是 给其中一个变量赋值，另一个也跟着变。
- C++里面有引用传递 JS没有引用传递 全是值传递。
 

## 纯函数
- 纯函数就是 相同的输入始终返回相同的输出，不受外部状态影响，也不修改任何外部状态，没有副作用。

## arguments
- argument是一个伪数组对象，表示所有的参数列表，现在用 es6 的剩余参数取代。
```js
function a(...args) {
  console.log( Array.from(arguments) )
  console.log(args)
}
```


## 闭包
- 闭包就是函数嵌套函数，内部函数可以访问外部函数的作用域。

    ■ 好处
      ● 好处是 可以延长变量生命周期 和 模块化
      ● 当内部函数保持对闭包变量的引用，GC就不能正常回收
      ● 闭包执行，形成私有作用域，不受全局污染，适合模块化开发，commonjs和esmodule都实用了闭包。

    ■ 缺点
      ● 可能导致内存泄露
      ● 不再使用的变量就赋值为null，切断引用让垃圾回收机制回收。
 
  ○ 讲应用
    ■ 闭包的应用场景有 防抖 节流 柯里化 vue中的data函数也是用了闭包
## 闭包变量

## 闭包区域


## 递归
- 递归就是函数自己调用自己，注意结束条件就行，不要形成死递归。

## 惰性函数 
- 惰性函数是 第一次调用时 根据实际情况 重写自身，后续调用更高效。这个实际情况后续都不会变。 比如 一些兼容性问题 只需要判断一次就行 不需要每次都判断。

## 防抖和节流 
- 防抖是：在指定时间内，多次触发都会重新计时，只执行最后一次。比如 搜索联想 
- 节流是 多次触发在指定时间内只执行一次。 比如 滚动条频繁滚动触发


## 参数归一化

## 函数重载
- 函数重载是 可以声明 函数名相同 但 参数个数 或 参数类型不同的同名函数。 js不支持函数重载 只能手动模拟。
- 函数重载：函数重载是 函数名相同，参数的个数或类型不同。

- java语言 可以直接 声明 函数名相同 但 参数个数或参数类型不同的函数。
- js不支持函数重载 但可以手动模拟。
```js 
function createOverload() {
    const map = new Map();

    function overload(...args) {
        const key = args.map(it => typeof it).join(',');
        const fn = map.get(key);
        if (!fn) {
            throw new TypeError('没有找到对应的实现');
        }
        return fn.apply(this, args);
    }

    overload.addImpl = function (...args) {
        const fn = args.pop();
        if (typeof fn !== 'function') {
            throw new TypeError('最后一个参数必须是函数');
        }
        const key = args.join(',');
        map.set(key, fn);
    }
    return overload;
}

const getUsers = createOverload() ;

getUsers.addImpl('xfsfd', () => {
    console.log(23)
})

```


