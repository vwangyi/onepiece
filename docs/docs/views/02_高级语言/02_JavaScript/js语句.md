
##  语句
-  语句是最小执行单元 也就是底层的一个二进制指令  单语句 以分号; 结尾

## 语句块
- 语句块 以大括号{}括起来 里面可以包含多个语句

## 流程控制语句
- 顺序结构 分支结构 循环结构

// 顺序流：从上到下 从左到右 一行一行执行（受运算符影响 比如赋值 ）
// 分支流：分支语句
// 循环流：循环语句

## 单行注释
多行注释
文档注释 非标准


## 循环
for
for in
for of 
while
do while


Symbol是es6新增的基础数据类型, 可以创建绝对唯一的标识符，不需要关注值本身是什么

const KEY = Symbol(); 
const obj = { [KEY]: 1, a: Symbol() }
vue中的key 可以传Symbol()

Symbol() 不会被遍历 for of forin Object.keys中拿不到symbol()
## js表达式
- js表达式是 能够计算并产生一个值的合法代码单元，能够作为函数参数传入，基本就是js表达式。