那审批节点不就是审批一，审批二，就类似于钉钉的审批，又不知道你用没用。
就是有几个请求，然后是不是？不是。
不是。是一是一个审批流程，需要不同的人审批，第一个过了到第二个，这就是审批节点，一级。
一级审批人和二级审批人那样子。
对对对对对，什么意思？然后子应用是什么呢？子应用就是 iframe 内嵌的，就是他们负责业务表单的展示，就是不同的审批人要填一些东西，说上报有可能是请假的，有可能是报销的，有可能是 PD 的，对吧？
同的审批人大概能理解。
那得填那个表单a，然后，然后。哎，你还有啥不明白的？咱们一个一个解决。
然后再往下那个 wiki 沉淀场景。
这我好像跟讲了 wiki 不就是那个啥嘛？一个文档库名字叫wiki，你可以叫它，其它的一般都wiki，就类似于语券组，就。
和那个精细做的那个精细文档那个差不多，是吧？
是差不多，只是他对做了一样的事。对，你可以把。
这个东西写了出来，然后后面的人去可以去看，然后可以节省沟通成本。
对对，就这么个意思。然后那些场景有一大堆，你可以把这个场景往里放嘛？精细化场景你也可以放一放。
行，那我就直接套那个理解就可以了。
然后沉淀场景其实就是一些经常错的东西，你可以把那个直接套过来就行。然后。
然后就是解决iframe，业务方就这 iframe 题。iframe，然后业务方有 4 个。
嗯，这个主要是什么？就 iframe 它有个问题，就是就是你知道有了解微签端吗？你看了半天你要了解吗？了解不了解。
知道它几个应用？iframe，乾坤。
干啥的知道吗？干啥用不干啥用的知道不？
这个倒没去了解。
卧槽，你得了解。我说你这，他给你讲这个就是一个微前端项目，你都得了解一下他是干啥的？微前端，微前端就是把两个应用集合在一起，你是你的项目，我是我的项目，你怎么嵌套我的项目？就微前端。
OK。
就是在一个页面里边既有你的项目又有我的项目，咱俩又是分开部署的，在不同部门的。我做的这些平台我对接了 200 多个， 300 多个业务系统，它都和我这个平台集成，通过 URL 上不同配置我配不同的，这个 URL 是后台传我的。
就类似于那个 view eruter 那样的。
还不太一样。我这个主要是后台传我的，你知道吗？就我这个 URO 上挂了一个 i d，其实就是类似于router。然后我把这 ID 呢发给了服务端，服务端告诉我这个 id 是哪个业务平台，我就把不同业务平台的页面展示出来，应该是有下载加载不同的URL。
就是通过 URL 改变不同的页面。对，是这么的。然后就是后面那 4 个问题都是怎么解决的？
哪 4 个问题？就。
CSS 兼容性问题，滑动不流畅，屏幕变大， IM 失效，是重新设置IM。
就是写死它的 font size。
font size 跟那个跟目录的 font size？对。
其实是 IM 失效，也是加一些属性来解决，其实那都是纯编，你知道？能看到那个好，有点大，这个你打开过吗？
这个是什么呀？
卧槽，这个就是这，第一个好像就是这个呀。你看没看这个文档？第一行就是这个脑图项目，脑图。
这个 XMIND 我打不开。
下载一个这软件叫叉还能力的软件。
OK，OK，OK。
这都写清楚了，这个 Bubble 怎么写？其实就是Bubble， Bubble 一个插件，插件里边有 4 个模块， Bubble car， Bubble travels， Bubble pass，遍历它的节点，查它的节点，遍历ST。其实这你都知道，对吧？
因为八宝的原理就是将源代码这换成AST，然后 ASC 里的进行，那。
金融损失是怎么做的？你看了吗？止损。
怎么做我不知道，但是我是对它进行了一个为什么要这么做的一个归纳，就是。
对，其实你知道为什么？就是前端不能做浮点计时计算嘛？然后怎么做？应用发布这个原理来做的。然后就是识别什么人民币，美元，英文这些东西，然后作为一个告警，类似于 yes link。
告警语法报错。
对，在 bubble 里做的大概就是你说这些模块就完事了，这都给你写了，怎么做的？然后然后这个就是我在加入夹儿里边儿做的一些东西，然后， iframe 会有一大堆问题。首先是历史回退，父子交互， UI 不同步， i frame 加载慢切入，以前闪动有的能解决，有的就不能解决，不能解决的其实，但是它不太影响，比如 iframe 慢，它不影响， Tob 就慢点。
对吧？没事，不能解决的，不能解决就不跟它说了，光说那些解决了的。
不不不，你还得说不能解决，最后你还得用前功来改造了。然后它你刚才说的是兼容性的问题，对吧？比如说无法操作滚动条，嗯，它一般都是在那个 iOS 低版本会出问题，比如说八九十新版本它不出问题，这是，但是你不能说强迫用户升级，这是一个 ToB 系统，所以就做一些自定义loader，还是用自定义 loader 来做的。加一这个属性就解决了字体变大的问题，因为它有支持横屏，然后黄的不跟手是加这个属性，css。
配置一些相关属性。
对，就是我只要识别的滚动条了，我就加这个。其实整体写法其实就是通过正则然后来配置的。然后这些 loader 都放到我的 webpack 上，不是放到我 iPad 上，都配到我脚手架上了。我的脚手架都给别的购物别的项目用了。
OK，标准脚手架。
对，这就是标准脚手架。 OK 了吗？现在是不是通了？嗯，然后我 iframe 的交互用 post message 做的， post message 就是交互的，交互用的。然后做了三个版本的，你就说三个版本，三个版本，就是交互。首先他得告诉，因为有时候他点通过按钮的时候，他得告诉我，他有时候就在他那触发通过按钮了。你比如说他那提交表单，啪，他那有按钮提交表单了，对吧？嗯，然后有时候这个流程他就讲通过了，他就不不用户点我这儿通过按钮了，啪它一点，我就自动就弹了一个二次确认的填一些意见修改建议，这包括了需要这种交互的。
那个上线收口是什么意思？上线收口？是啊，他们那不是直接用成品了？那。
用这样的，我这个标准脚手架不有两个亮点，刚才给你讲了，对吧？对接组件库你也得看一下。组件库你知道吧？
知道，就是自公司自己写的一些组件。
对，然后他对接了我这个结果其实非常简单，就赢了就行了。然后做了一些，就是可能是那个叫什么按需加载那种东西，然后去导入按需加载，然后 wiki 场景加展，就是类似于我刚才那个这个东西，然后上线收口机制是啥？它是一个机制，它这个机制啥意思？就是所有的发版都在我这儿，都需要我这儿通过。
没有发版。
他上线都要走我这配置流程啥的，就是上下权里更改。
一些设置，更改设置。
但是其实就是类似于URL，它不在我这展示页面，URL，它不配的 UR 它看不了线，它看不到它的页面儿，对吧？懂我。
意思，它这个大整体是把一个框架就和那个什么搬入，就跟那个 VV 框架一样，给他们一个框架，然后他们去拿着框架去进行二次开发。
不不不，不是这样的，不是这样，你了解一下微前端，微前端就是一个项目，他给我一个URL，我放上去就展示他们项目了，比如这个暗号，这个框，这个都是，这是咱们项目，对吧？它内容是他们项目，那如何？我让我这个项目嵌入他项目，对吧？那就是他给我URL，我直接用 iframe 在这展示，这是我项目，这是他项目。
OK，OK，我一会去搜一下，OK。
上线收口机制，就是什么？它必须给我 URL 再展示才能展示它这个项目，要不然它展示不了，然后我们还在上下做一个检查，就是还是就是让他将这个 wiki 场站、场景这个都打勾，都确认了，他测完了，懂吗？比如说什么 i e 问题， IE 他们经常不测，对吧？低版本问题，对吧？他们都不测，你看这都写了吗？ wiki 做题机制，这举了三个商业收狗， IELSE 版本，谷歌浏览器 85 的一些问题，然后还有缓存问题，你随便搜几个问题往上一摞就行了。OK，又说这是你们常见的问题，直接进一搜，一大堆问题你就随便说几个就 OK 了。不一定非得跟我这一样，反正它功能是这么回事，通过这三个机制来解决整体的问题。然后这个难点一觉得 OK 了没有？嗯，可以好，然后你难点二没讲是吧？讲。
了吗？没有哦，那个视频里没讲。
我应该讲了就选怎么选型。讲了。
吧，选型只讲了一个，那个对，就问题，就微前端选型优劣就这一块。
其实你可以照抄了。
嗯，这个我的确照抄了，背完了。对对。
就是就按照这个嘛，你意思为什么要选前关，对吧？首先你得知道为什么不选FM。
的选项，我们当时有这么多选项，然后我再把那几个优势说一下，然后但是我们最终选择了乾坤。然后因为是比较成熟，并且是基于 single SPA 的优化。
single。
s p。
a，哎，对对对对对，你就照着这个背就行，然后带着前观音有些不足，你看不足也给你写了 i e 样式隔离有问题， shadow DOM 它不支持 ie 隔离，所以我在这么写的，用这个来做的。然后你查一下这个原理是啥，它其实也就是在那个用 loader 来让你的那个加个前缀。
okay，就实现了。
颗粒，然后跨越，话语就比较麻烦了，我想想，还有我把这优化一下，把这跨域和这个什么优化一下和登录后边三个先优化一下，然后你先把这上面整一整，我把后边的整完之后给你们录个视频，好吧？
行，然后军哥你先说。
然后你项目成果这个你也看一看，项目成果我看看连这都写了，这块儿相对比较复杂，这块儿就涉及到的这个东西比较多了，我给你大概一聊一下这个。
一下这个流程。
嗯嗯嗯，聊一下这个大概流程其实是啥呢？就是查一个失败的复盘，我查一个项目的收益，就是跨域是啥意思，你知道跨域是啥意思吗？就比如说他的项目肯定是他的域名，对吧？对，那他在他的域名下，他其实他的资源都是他的域名，然后他访问的后台又是他们的后台，就是，你懂我意思吗？两个项目，两个域名，但是他访问他后台，在我的页面里它就是跨域了，是吧？因为页面只有一个，我拉他只是拉他的资源，但是他访问他后台就跨域了。那怎么解决这跨域呢？其实就得用 allow origin 来做 a region，它喻有 n 多种方式，其中这个方式是比较好弄的。 allow a region，然后写一下就是针对我的项目的域名做一个快域设置。
开启一下那个源，允许那个源通过。对。
对对，就是那个东西，但是这个我不可能让每个后台都设置，所以我在网关层。哦，你就跟面试官说网关层他就知道，然后查一下网关层是什么。c，其实一个 spring cloud 的一个服务器公司都用运维把这个他也不懂，就把关做一个这个就 OK 了，OK，然后把我们所有的这个子应用注册一下，就是但凡是这些服务器对这个源都设置 all region 就完事，就解决了跨域问题。
就是我在我们网关层设置了一个 our orange。
allow origin。
allow origin allow allow origin。
我们我在我的网关层设置了个 allow origin 这个东西。
那个属性是我页面的源。
配置上了，我页面配置到我。
what yuan 我来这边不是可以设置域名吗？
我自己域名的源。
就其实就是自己的域名就好了，这样所有的这种访问都没事儿，就经过这个服务器的。懂，你懂这个意思？
我意思我懂，就是自己去首先在网关那设置自己的源，然后自己的源再去访问，那肯定就可以通过。
对，就这么回事，就这么回事，然后我就登录了。卧槽，登录就麻，蛮麻烦了。我感觉给你讲不明白，你看一下吧。这个谷歌 85 有没有聊过了解过那个 same set 属性。
set 不是 Cookies 那个跨域设置吗？
对对对对对，就是它浏览器碰到谷歌 85 了，它就会有问题，碰到微前端。
知道吧？嗯，不允许它通过。
就是子应用的那些请求，它和我主应用的页面的域名不一样，就是跨域了。跨域那 SIM set 就有问题了。
是给它设置一个非严格模式吗？
对，有这种方案，但是这个得联合 SSO 来做，就是登陆，就是我们朋平时种就是种cookie，那个部门要种 n 多个cookie，就我因为我们域名有很多，有 6 个玉米儿或者 3 个玉米儿，然后它重呼吁的时候，它得循环跳，跳一个中一个，跳一个中一个跳一个，它再跳个四五下，然后把不同域名种上这个cookie，同时它设置这个属性，它就解能解决。
第一层拦截，就是把那个 swim set 属性给它种上，因为它不允许跨域种 cookie 了。然后因为登录它会有两层拦截，一个是 cookie 层的拦截，一个是浏览器层拦截。浏览器层拦截是怎么做的？也是在网关层设置两个东西，一个是 with creative 等于true，还有一个是 allology 等于动态的请求头，因为这时候他拿不到我当前的源，它要动态设置。我觉得你把原话理解了，然后跟面试官说去，你不用理解，跟他说他也懵逼。
对，一般不接触这个，猛地一听的确挺懵的。
对，你可以理解，不会问我就行了。然后。
网上这个六连跳是每进入到一个页面就把它那个页面，那个，就那个网关层的那些东西给它修改一下吗？还是给它不是设。
网关层？ 6 栏跳是重cookie，这是单点登录，就是你当前页面要有cookie，这个你必须得会，就是单点登录就是你中cookie，但是它这谷歌这个新浏览器它不让你跨域种了，原来它一下中 10 个，它现在不让它跨域中了，它只能一边儿跳一边儿种，就六连跳，每次跳个页面重一下，把所有页面都重上，有点儿com，有点儿cn，点儿com，叫net。对，你把这个话术整理整理，然后结合这脑图，然后呢？你不会问我，我觉得第一次讲你肯定懵逼，然后依赖公用，你先跳过，你先把这几个讲了，我觉得面试官就懵了，你先整整好，然后我也整一整，然后把这个吸收。
就是军哥，还有就是就这个项目刚开始是用的是IFIRM，后边是改用乾坤。
对，是吗？对。
然后然后就再往后就是说乾坤有一些不足的之处，然后拿，然后再对这些不足之处我再给他进行一个优化。诶，这是难点二的。对，然后难点一其实是针对于是业务对接太多的解决问题，然后难点 2 就是对于那个改造FM，改造乾坤了，然后乾坤上有的一些问题进行。
对，你这个归纳能力很强，我跟你说，你光跟面试官把这个两个问题抛出来了，面试官就觉得你有水平，然后你在结构化的一个表达，对，因为他都不。
知道问啥细节化，我感觉我记不了那么多。我，所以我先把大纲捋出来，然后细节化再往里。
对，我跟你说，你有这个套路，我跟你说你甚至比渊总还强，你知道吧？渊总有点太急了。
军哥，冤总他那个是最近。
怎么样？冤总那个还给我介绍学员呢，怎么样？袁总接了一个史山，袁总拿了他妈四高分，你知道吗？哈哈，第一个12K，第二个9K，第三个11K，第四个13K，他最后去13K。
他那个公司他那个业务能弄下来吗？
知道，应该没问题。
没问题不难吗？用 Voe 做的。
Voe 做就是我跟你说你照着这个你好好学，我跟你说你你就好好背。我觉得你 20K 都能拿到。
你信不信？我感觉我三年是拿不到20K。我感觉 20K 像这个行情，因为毕竟是魔法本，要是自己实打实的本我估计还可以，但是魔法本我估计就是。
只能这么说。确实是去小公司只能去小公司，小公司就。
实力他不查。嗯， 16 期对，而袁总还写的 react 和ant。什么袁总什么都写。袁总简历。
啊袁总简历啊，然后你整整，我觉得你能行，他搞他，因为他后面没学，他光学前面了，他有点着急了。
我觉得袁总已经自己脑补的一些东西上去了。
大文件上传，你搞没搞？
大文件上传大陆现在看这个快。这个话术什么嘞？我之前看过这个快，那个杜一啊。对，那个杜一好像讲过这个大文迁得出来。
把大文迁出来，先朝着整理一个，然后把这个也整理。
一个。我今晚上就先把大文件先让它整理一版，然后我把。
它你写到语券上，我可以给你，我给你那个丰富一下，你写语券上把你整理好的，然后这个你也整，你把这三个整了，我跟你说这个军哥实力，军哥靠这个这些东西你知道吧？嗯，军哥能达到70。好吧，但是我还有项目，你知道吧？我还有项目，我还有东西没写呢。我这有一个就是你们写都看不懂，所以我就没写那个。你看这个前置监测平台，你知道这干嘛的吗？说了你都听不懂，你知道吗？这做端到端测试的没听说过。
名词？听说过端到端测试这个名词。
是自动化测试的一种。自动化测试分为两种，一种是端到端测试，一种是单元测试。
前端还得做自动化的测试。一般做这个不都是 Python 什么 c c 什么的，C。
+ + NODE，这个拿 NODE 是弄的。
牛，哈哈，军哥牛。
还有监控，你看还有监控，还有组件，卧槽，就是确实。
监控，这个我这个知道，因为很多东西地方做那些视频监控，拿那个小程序就搞这个，当时我做比赛的时候就有个人弄这个来。
这个项目就纯体现你的这个业务，然后第二个项目是体现你的这个技术，第三个项目体现还是技术，第四个体现的你这个工程化能，所以它这个简历是挺饱和的，不饱和的，然后这块又够牛逼，所以就能拿到。
70。我准备就是一个审批平台，一个牛逼的，然后剩下三个，两个是特别水的，扔最后面，然后第二个，第二个就写的写个游，找个游戏项目放上去，然后给它补一点，就把那个 EX Excel 的 bug json，对放上去，然后再写一些。
数据，对，然后靠着这我还有一个要给你们，你知道就我经常给你们讲的那个你，我不知道你听没听，反正就是那个也是游戏项目，就是就是那个啥，就是整一张图片整进去的一个。
平台。那个不是那个那 70K 大佬的，他给他讲的那个嘛？那个我听了，但是。
那个要装进去。
那个也装进去吗？不是。
70K，那是 70 万大佬。那 70K 大佬那他妈过百万了。华北。
的。华北年纪是吧？行行。
OK，OK，那就先这样。嗯，好好学，有问题问。好的好的。嗯，关键是你整理我都会看。
嗯嗯，好好拜拜，徐哥，再见。