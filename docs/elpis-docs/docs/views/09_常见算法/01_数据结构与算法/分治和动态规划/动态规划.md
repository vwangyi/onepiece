

- 动态规划是什么
- 状态转移方程是什么 状态是什么
- 
## 动态规划
- 状态是什么
- 状态转移方程是什么
- 动态规划为什么叫动态规划：动态是指状态会改变 规划是指最优的计划 
- 现在当你说"动态规划"时，你脑子里应该浮现： 
- 一个动态推进的过程 + 一个精心规划的最优方案
 
动态规划有自底向上和自顶向下两种解决问题的方式。自顶向下即记忆化递归，自底向上就是递推。


状态转移方程是什么：不同规模的相同问题之间的关系 就是 状态转移方程

找到相同问题（重叠子问题），相同问题必须能适配不同规模。
找到相同问题之间的关系（状态转移方程）
找到相同问题的特殊解

f(1) dp(1) // dp就是动态规划简称
 

既然同样的子问题要算很多次，为什么不只算一次，然后记住结果？


动态规划四步法（JS版）
第1步：定义状态（最重要！）
// 状态就是"我们要记住什么"
// 通常用数组 dp[] 或 dp[][] 表示

// 例子：爬楼梯问题
// 状态定义：dp[i] 表示爬到第i阶楼梯有多少种方法

// 例子：背包问题  
// 状态定义：dp[i][w] 表示前i个物品，背包容量为w时的最大价值



第2步：状态转移方程（核心公式）
// 如何用"小问题的解"得到"大问题的解"？

// 爬楼梯：dp[i] = dp[i-1] + dp[i-2]
// 意思：到第i阶 = 从第i-1阶走1步 + 从第i-2阶走2步

// 最长递增子序列：dp[i] = max(dp[j]) + 1 (对所有 j < i 且 nums[j] < nums[i])
// 意思：以nums[i]结尾的最长序列 = 前面比它小的序列中最长的 + 1




分治和动态规划的区别是 动态规划是用空间换时间 已经计算过的就记录下来
```js
function solveDPProblem(input) {
    // 1. 定义状态（写下来！）
    // dp[i] 到底代表什么？要清晰明确
    
    // 2. 初始化边界（最简单的情况）
    // dp[0] = ? dp[1] = ?
    
    // 3. 状态转移方程（写公式）
    // dp[i] = dp[i-1] ? dp[i-2] ?
    
    // 4. 确定计算顺序
    // 从小到大？从大到小？
    
    return dp[结果];
}

const practiceProblems = [
    { name: "爬楼梯", difficulty: "简单" },
    { name: "斐波那契数列", difficulty: "简单" },
    { name: "打家劫舍", difficulty: "中等" },
    { name: "零钱兑换", difficulty: "中等" },
    { name: "最长递增子序列", difficulty: "中等" },
    { name: "编辑距离", difficulty: "困难" }
];

// 建议顺序：先做简单的，理解基本思想
```

## 斐波那契数列
```js
// 分治解决斐波那契数列（笨方法）
function fibDivideConquer(n) {
    if (n <= 1) return n;

    const a = fibDivideConquer(n-1);
    const b = fibDivideConquer(n-2);
    return   a + b;
}
 
```




● 将复杂问题分解为简单的子问题，通过解决子问题来解决原问题。 
● 重叠子问题
● 是什么：将复杂问题 拆分为 简单的重叠子问题，通过解决子问题来解决原问题。


动态规划是 把原问题拆分为 多个子问题，先求解子问题
将复杂问题分解为简单的子问题，通过解决子问题来解决原问题。

动态规划（Dynamic Programming，简称DP）是一种解决复杂问题的算法思想，它通过将问题分解为相互重叠的子问题，并存储子问题的解来避免重复计算。


● 1，1，2，3，5，8，13 这就是斐波拉契数列  观察规律得知：从第二位开始 后面的值 都是前两位之和
● 
● f(5) 表示 斐波拉契数列 第 5 位的值是 5
● f(6) 表示 斐波拉契数列 第 6 位的值是 8
● f(7) 表示 斐波拉契数列 第 7 位的值是 13 
● f(n)  n 就是规模  这就是叫 不同规模的相同问题
● f(n) = f(n-1) + f(n-2)
● f(7) = f(5) + f(6)
● 特殊解 f(1) = 1 
● 特殊解 f(2) = 1

● 斐波拉契数据 是把规律拍你脑门上了 一眼就看出来 其他很多题需要自己寻找 没这么简单
1. 寻找相同问题（重叠子问题）相同问题必须要适配不同的规模
2. 找到重叠子问题之间的关系 不同规模的相同问题之间的关系  
3. 找到重叠子问题的特殊解


动态规划（英语：Dynamic programming，简称 DP）是一种在数学、管理科学、计算机科学、经济学和生物信息学中使用的，通过把原问题分解为相对简单的子问题的方式求解复杂问题的方法。

动态规划常常适用于有重叠子问题和最优子结构性质的问题，并且记录所有子问题的结果，因此动态规划方法所耗时间往往远少于朴素解法。

动态规划有自底向上和自顶向下两种解决问题的方式。自顶向下即记忆化递归，自底向上就是递推。

使用动态规划解决的问题有个明显的特点，一旦一个子问题的求解得到结果，以后的计算过程就不会修改它，这样的特点叫做无后效性，求解问题的过程形成了一张有向无环图。动态规划只解决每个子问题一次，具有天然剪枝的功能，从而减少计算量。

以下是动态规划的相关练习，可以帮助你进一步了解动态规划，快来试试吧！


斐波那契数列 的状态转移方程 dp(n) = dp(n-1) + dp(n-2) 这个一眼看出来的
爬楼梯问题  的状态转移方程也是  dp(n) = dp(n-1) + dp(n-2)  这个是推理出来的
力扣 62.不同路径


```shell 
# 第一周：基础
# 1. 斐波那契数列
# 2. 爬楼梯
# 3. 杨辉三角

# 第二周：一维DP
# 4. 打家劫舍
# 5. 最大子数组和
# 6. 零钱兑换

# 第三周：二维DP
# 7. 最小路径和
# 8. 最长公共子序列
# 9. 编辑距离

# 第四周：高级DP
# 10. 背包问题
# 11. 区间DP
# 12. 状态压缩DP
```