问自己这几个问题：

function isDPProblem(problem) {
    const questions = [
        "1. 问题是否要求最值（最大、最小、最长、最短）？",
        "2. 问题是否能分解为子问题？",
        "3. 子问题是否大量重复？",
        "4. 子问题的最优解能组成原问题的最优解吗？"
    ];
    
    // 如果大部分回答是"是"，那就用动态规划！
    return questions.filter(q => answerIsYes(q)).length >= 3;
}

// 典型动态规划问题特征：
const dpCharacteristics = {
    "求最值": "最大利润、最小代价、最长序列等",
    "求方案数": "有多少种走法、组合方式等",
    "求可行性": "是否能达到目标、是否满足条件等"
};