# 部署  


## 本地部署


## 云服务器部署 - 轻量云服务器
- nginx 部署前端静态文件
- pm2 部署后端nodejs项目 docker k8s 

## 云服务器部署 - 企业级云服务器






 
## 部署

## 前置条件
- 购买云服务器
- 








## 域名备案
+ 域名映射 IP 就需要完成备案



## nodejs一般怎么部署上线？
1. 单机适合 PM2 （适合中小项目自己玩，用户量不大）  轻量云服务器
2. 集群，大型应用   云平台  K8S容器编排。  云服务器 集群 云数据库 

## PM2的应用场景和功能
1. 进程管理：PM2可以启动、停止、重启和删除Node.js应用程序进程。它可以管理多个应用程序，并确保它们在服务器上持续运行。
2. 自动重启：如果应用程序崩溃或发生异常退出，PM2可以自动重启它，以确保应用程序的高可用性。
3. 负载均衡：PM2支持负载均衡，可以将请求分发给多个应用程序实例，以提高应用程序的性能和可扩展性。
4. 日志管理：PM2可以收集和管理应用程序的日志输出。它可以将日志输出到控制台、文件或日志管理工具（如Elasticsearch、Logstash等）。
5. 监控和指标：PM2提供了监控和指标功能，可以实时监视应用程序的CPU使用率、内存使用率、请求响应时间等指标，并生成报告。
6. 环境变量管理：PM2可以管理应用程序的环境变量，可以在启动应用程序时设置环境变量，并在应用程序中访问这些变量。
7. 部署和更新：PM2可以帮助您部署和更新Node.js应用程序。它可以从代码仓库中拉取最新的代码，并自动构建和部署应用程序。

总结来说，PM2是一个功能强大的进程管理器，可以帮助您管理和监控Node.js应用程序的运行。它提供了进程管理、自动重启、负载均衡、日志管理、监控和指标、环境变量管理以及部署和更新等功能。

## 后端 CICD 部署


+ 方案一： 单独购买 云服务器、云数据库、OSS对象存储服务器 至少这 3 个东西
    - 缺点：贵 （企业使用）
+ 方案二：购买 "轻量应用服务器"   不买数据库 直接安装 mysql到轻量应用服务器上、OSS 对象存储服务器 如果便宜可以单独购买、 轻量服务器上 安装 nginx服务器 部署前端项目
    - 缺点：费用大概 80 一年（个人或小企业使用）
    - 购买轻量服务器 有可能免费送 OSS 和 云 mysql



+ 购买流程
    - 选择镜像 也就是选择系统 一般是用 centOS 7.x 
    - 

```markdown
我想部署上线 但 云服务器 云数据库 OSS对象存储服务 都太贵了 怎么办
降级 购买 "轻量应用服务器" 数据库不要单独购买 直接安装 mysql到轻量应用服务器上  OSS用免费的额度
购买尽量采用 学生优惠
"云服务器" 和 "轻量应用服务器"的区别就是 轻量应用服务器就是云服务器阉割版 面向个人开发者
轻量服务器上 安装 nginx服务器 部署前端项目


购买轻量应用服务器 直接买也比较贵 就看有活动的 大概 200 块左右一年 

我见过阿里云活动 2 核 2G 的只要 38 元一年 太便宜了 
有服务器才可以多人协作开发   没有服务器本地别人没法统一多人协作

war 包
jar 包（java 打包出来的就是 jar 包）
流程

1 开发完成 
git提交  
编译（npm run build）（得到一个dist文件夹）   
 zip打包 得到一个 zip包或jar包
前端


运维或后端
把jar包放到服务器上 
拉取下来解压 放到 nginx上  pm2运行 



jkens
https://www.jenkins.io/ 官网
https://www.k8stech.net/jenkins-docs/basics/introduction/ 中文

docker
https://www.docker.com/ 官网
https://www.dockerdocs.cn/get-started/overview/index.html 中文


我们写好的代码 之所以可以在我们的电脑上跑 那是因为 我们手动装了环境 比如 nodejs 浏览器等等
而 我们写好的代码 要在 docker镜像容器 上跑  需要写一个 dockerfile 来装环境 比如 nodejs 等等
镜像仓库 存放了 很多docker镜像


jenkins 流水线  表示 git close 到 docker打包 这一流程

运行到 nginx 上 就是 运行到 linux系统 


构建过程（ CI ）:  git clone   npm i   npm run build  得到 dist    执行 dockerfile 文件  得到 docker镜像  


k8s: 开源 编排的容器平台。把每个 docker 镜像 运行起来 




开发应用 可能也会修改elpis框架 所以 使用 npm link @codewy/elpis 
```


 
 

```markdown
/**
    validate-commit-msg ghooks 
    feat: 新功能（feature）
    fix: 修补bug 
    docs: 文档（documentation）
    style: 样式（不影响代码逻辑运行的变动）
    refactor: 重构 
    test: 增加测试 
    chore: 构建过程或辅助工具的变动 
 */
``` 
 

## CI 持续集成
+ git push 
+ 编译 npm run build 
+ zip打包
+ 从git push  >>> 得到一个zip包

```javascript
设计到 jenkins

git push 

1 git clone 
2 pnpm i 安装依赖
3 npm run build 编译
4 docker 打包

概念1 查看jenkins官网
概念2 查看docker官网（docker是一个容器 相当于是一个电脑 可以下载node环境等等）
概念3 镜像仓库 里面有多个docker容器 
```

## CD 持续部署
+ 上传服务器节点
+ 解压
+ pm2运行 / 放到Nginx 





```javascript


代码托管平台 

阿里云 腾讯云 亚马逊云 


1 检出代码 （git clone）
2 安装依赖  pnpm i 
3 打包构建 npm run build
4 构建docker镜像 执行Dockerfile  把docker推送到制品仓库




k8s集群管理是一个开源的容器编排 
woker节点 就是一台服务器电脑 （需要花钱购买） 会提供一个ip地址 提供给用户访问


k8s有很多用处 
  比如说 
    需要发布版本 不可能让用户有10几秒不能访问 而是分很多节点
    一部分节点先更新 对于用户来说 某些用户在同一时间点 先访问到新版本 有一些后访问到新版本而已
    这就是滚动发布  
  比如特殊节日 双12 618 流量很多 这个时期可以多增加节点 来处理请求 负载均衡

1 注册阿里云账号 
2 充钱 100元 
3 云产品 容器服务
4 创建集群（集群名名称 集群规格选最低 操作系统选） 集群需要使用钱 0.12元/h

```





## 是谁
```javascript
1 在阿里云 买 云数据库mysql  得到一个 数据库 账号密码 
  （记得开启外网地址 并且安全组设置规则 才可以连接数据库）

2 数据库实例里面有 登录按钮 点击进去 输入账号密码 登录进去


库里面就是表（表就像excel表）

新建库（elpis-bate） 有几个环境就有几个库 比如 测试是elpis_beta 生产是elpis_prod


建表 t_user  (用户表 t_前缀表示table )


建字段
 1 id 自增 主键 唯一标识一条数据
 2 user_id 唯一标识一个用户 （为什么用user_id来标识用户 而不是id 因为安全）


逻辑删除 而不是物理删除

user_id 一般是生成的uuid 	



```


 