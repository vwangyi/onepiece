# v8引擎

 

- 什么是js引擎
  - js引擎是用来解析js代码的工具

- 什么是V8
  - js引擎有很多，只需要学习v8就行
  - v8引擎是google浏览器和node环境用来解析js代码的。



  js 引擎有很多。 v8 引擎是 google 浏览器环境 和 node 环境 用来解析 js 语法的引擎。


# AST抽象语法树

（token就是不可再分的词法单元） 
抽象语法树是 把源码字符串 通过 词法分析 语法分析 得到 AST抽象语法树。


比如 var a = 1; 词法分析得到 5个词法单元token，分别是 关键字var 标识符a 标点符号= 数值1 标点符号; 


词法分析就是 输入源码字符串 输出5个词法单元token流，分别是 关键字var 标识符a 标点符号= 数值1 标点符号; 
语法分析就是 输入token流 输出AST抽象语法树，
语法分析就是把之前的token流 lianlian


最终形成 

eslint prettier babel vue的tempkate模版 v8引擎 都用了抽象语法树
```js
var a=1;
var b = 34;
function addA() {
    return a + b;
}
var c = addA(2) + b;
```  
拆分出 一个个的token（比如 关键字 标识符 操作符 ）
为什么是树结构 而不用链表 数组等结构 ：因为树的好处是 层次关系 搜索效率  动态数据集合 有序存储 空间优化 分治策略 
https://www.jointjs.com/demos/abstract-syntax-tree 可以把js代码转AST 
具体语法树（Concrete Syntax Tree） 也叫 分析树（parse tree）
抽象语法树（Abstract Syntax Tree）
空格 分号 忽略掉




后续步骤
语义分析：检查变量是否声明、类型推断、作用域绑定等。
AST 转换：修改树的结构（如 Babel 插件遍历 AST 并增删改节点）。
代码生成：从 AST 重新生成源代码（比如 Babel 将现代 JS 转成 ES5，或 Prettier 重新格式化代码）。

V8引擎 AST抽象语法树 GC垃圾回收机制

 
执行上下文 里面有 词法环境  

渲染主线程 调用V8引擎 去解析js代码生成字节码并执行 （主线程是人 V8是工具） 负责“算逻辑”，不负责“画像素”。
渲染主线程 调用Blink / WebKit 渲染引擎 去执行渲染任务， 负责“画像素”。（样式、布局、绘制、合成）。



事件循环


网络拿到 js css html 等资源
浏览器主线程 调用 v8引擎 执行js 
浏览器主线程 调用 渲染引擎 执行渲染 


事件循环


浏览器进程会启动 多个进程 比如 渲染进程

浏览器会打开一个浏览器进程，浏览器进程会开启多个进程 比如 渲染进程
渲染进程会开启一个渲染主线程，渲染主线程又会开启多个线程

渲染主线程 负责 调用 V8引擎 执行js  调用渲染引擎触发渲染 


渲染主线程上 有事件循环

渲染主线程要做的事
1. 根据 帧率 每秒渲染60次
2. 调用 v8引擎 执行js代码
3. 调用 渲染引擎 执行渲染

渲染主线程要做大量的事情 如何调度任务？ - 事件循环（排队）



html css 浏览器 都是 w3c指定标准 


其他线程（包含其他进程的线程 都可以往 事件队列 追加任务）

实现异步编程 


js是单线程语言，如果采用同步，就会导致主线程阻塞 没有及时渲染，所以采用 异步，实现异步的方式就是 事件循环排队


h1.textContent = 'dsf' // 主线程调用 v8执行代码 内存中同步修改值 并产生一个渲染任务去排队 
function delay(t) {
  const now = +new Date();
  while(+new Date() - now < t) {console.log(1)}
}
delay(1000) 
主线程调用 渲染引擎 执行渲染任务

渲染和执行js 都在主线程上 所以 执行js 一定会阻塞渲染

function delay(t) {
  const now = +new Date();
  while(+new Date() - now < t) {console.log(1)}
}
 delay(1000) 


h1.textContent = 'hello';   
function delay(t) {
  const now = +new Date();
  while(+new Date() - now < t) {console.log(1)}
}
delay(1000) 
第一句 主线程调用v8执行js代码 同步修改内存中的值 并产生一个渲染任务进行排队
第二句 声明一个函数
第三局 执行函数 死循环1秒
然后 主线程拿到 渲染任务 调用渲染引擎执行渲染任务

是这样理解吗 是的

1. 清空微任务队列
2. 如果有渲染任务 则会执行渲染 
3. 取下一个宏任务

问：渲染任务和宏任务 也在一个队列里面排队吗 

微任务队列
交互队列
延时队列
任务没有优先级 先进先出 
但任务队列有优先级 在 w3c中要求 必须要求有微任务队列且微任务队列优先级最高  

宏任务队列分为多个队列 比如 交互队列 延时队列
至于 第二高 第三高 是哪些队列 由浏览器自行决定。 chrome中 第二高是交互队列 第三高是延时队列





面试题：阐述一下JS的事件循环
参考答案：
事件循环又叫做消息循环，是浏览器渲染主线程的工作方式。
在Chrome 的源码中，它开启个不会结束的 for循环，每次循环从消息队列中取出第个任务执，其他线程只需要在合适的时候将任务加入到队列末尾即可。
过去把消息队列简单分为宏队列和微队列，这种说法目前已无法满足复杂的浏览器环境，取代之的是一种更加灵活多变的处理方式。
根据W3C官方的解释，每个任务有不同的类型，同类型的任务必须在同一个队列，不同的任务可以属于不同的队列。不同任务队列有不同的优先级，在一次事件循环中，由浏览器行决定取哪个队列的任务。但浏览器必须有一个微队列，微队列的任务一定具有最的优先级，必须优先调度执行。



单线程是异步产生的原因
事件循环是异步的实现方式 JS是单线程的 



使用异步的方式，渲染主线程永不阻塞
面试题：如何理解JS的异步？
参考答案：
JS是一门单线程的语，这是因为它运在浏览器的渲染主线程中，而渲染主线程只有一个。而渲染主线程承担着诸多的工作，渲染页面、执行JS都在其中运行。
如果使用同步的式，就极有可能导致主线程产生阻塞，从而导致消息队列中的很多其他任务无法得到执。这样一来，会导致繁忙的主线程白白的消耗时间，另方导致页无法及时更新，给用户造成卡死现象。
所以浏览器采用异步的式来避免。具体做法是当某些任务发时，如计时器、络、事件监听，主线程将任务交给其他线程去处理，身即结束任务的执，转执后续代码。当其他线程完成时，将事先传递的回调函数包装成任务，加到消息队列的末尾排队，等待主线程调度执。






后端：开发源码vue 到 生产源码js  前端工具链 构建器

前端：浏览器打开到关闭浏览器 执行 js html css 等


渲染render是什么：输入html字符串 输出像素信息








输入url到页面渲染发生了什么
网络部分：拿到html
渲染部分：渲染html 


打开浏览器 >>> 自动开启一个浏览器进程 >>> 浏览器进程开启一个网络进程 >>> 网络进程开启网络线程 发请求 拿到html文件

主线程拿到html文件做的事： 就产生一个渲染任务，追加到 消息队列 进行排队 
在事件循环机制下 主线程拿到渲染任务：调用 渲染引擎 执行渲染 

渲染引擎如何渲染呢？ 渲染过程必须是串行的

8个步骤  输入 和 输出 

1. 解析html：输入html 输出 DOM树和CSSOM树  
  DOM树就是控制台的document选项卡 通过 console.dir(document); 可以打印出来
  创建dom需要上树 也是挂在到 document这个对象上面  我们操作dom 也是 document这个树上的dom

  CSSOM树  console.dir(document.styleSheets); 
  document.styleSheets[0].addRule('div', 'border: 2px solid red !important')
  StyleSheetList 根节点 一共有4个样式表   【外链样式表link标签 内链样式表style标签 行内样式表style属性  浏览器默认样式表】
  子节点就是 4个 ，如果写2个link 那就是5个 

  为什么有这一步 因为操作树结构对象 比 字符串方便    这一步要重点讲解



2. 样式计算：输入 dom树和cssom树 输出 带有样式的dom树 ， 带样式的DOM树就是控制台的document选项卡


3. 排列布局，输入 带样式的DOM树 输出 布局树：计算每个DOM的几何信息，  DOM树和布局树 是不一样的 比如 display：none在 布局树中不存在 在dom树中存在  再比如 before伪元素 在dom树中不存在 但在布局树中存在 ，  document.body.clinetHeight 就是布局树的尺寸信息


4. 分层 输入 布局树 输出的是 分层的布局树 

5. 



渲染阶段：
1. 判断是否需要渲染 （若不需要渲染 当前阶段立即结束）
2. 执行requestAnimationFrame 回调
3. 计算样式 布局 绘制 合成


清空微任务队列 再执行一个宏任务 









理解了这个顺序，你就能解释很多现象：比如为什么 while(true) 死循环会让页面卡住（因为永远到不了渲染阶段）；为什么 Promise 先于渲染输出；为什么 requestAnimationFrame 在渲染前执行等。





